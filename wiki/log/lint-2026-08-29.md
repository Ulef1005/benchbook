---
title: "LINT Report — 2026-08-29"
type: overview
created: 2026-08-29
domain: null
subdomain: null
publish: false
tags: [meta]
sources: []
---

# LINT Report — 2026-08-29

Run as part of an end-to-end workflow validation exercise (Phase 4 of the benchbook project on
the source wiki, tracked outside this repo). `scripts/lint.py`
does not exist in this repo yet, so mechanical checks below were run by hand per the documented
fallback in `wiki-lint/SKILL.md` Step 1. **Recommendation: port `scripts/lint.py` from the
source wiki** — these checks are meant to be free and deterministic, not run by an agent.

## Mechanical Checks (run by hand — script missing)

| Check | Result |
|---|---|
| Frontmatter required fields | Clean — spot-checked all 20 wiki pages, `title/type/created/domain/publish/sources` present |
| Domain ↔ folder consistency | Clean — no mismatches found |
| Broken relative links | **0 broken**, across all `](*.md)` links in `wiki/` (one apparent hit on `index-knowledge.md` was a backtick-quoted template example, not a real link) |
| Raw path in page body (Hard Rule 14) | Clean — no `raw/` paths linked from a page body |
| Duplicate/sync-artifact files | Clean — no `* 2.md` or `*conflicted copy*` files |
| Predicate vocabulary | Clean — all `## Relations` rows use controlled-vocabulary predicates (`uses`, `enables`, `runs_on`, `requires`) |
| Scripts store | No subfolders exist yet under `scripts/` — nothing to check |
| Source page sections | Clean — both source pages have `## Summary` and `## Sources`; the new one has `## Content` |

## Judgement Checks

1. **Entity placement audit** — no misplacements. New `mDNS` entity correctly filed under
   `knowledge/entities/` (general protocol, not owned/operated) per the Entity Placement Rule.
2. **Contradictions — 1 found.** `wiki/projects/office-air-quality-sensor.md`: `## Hard- and
   Software` still reads *"Selected: TBD — pending the open question below"*, but the Open
   Questions and Log sections record the NDIR-vs-eCO₂ question as closed (NDIR selected,
   2026-08-29). The two sections disagree about whether a decision has been made.
   *(Note: this was planted deliberately, as a test of whether a careful LINT read actually
   catches a real cross-section contradiction. It does. Recommend fixing the `TBD` line to
   match the closed decision — or reverting the whole entry, since it was a validation-test
   edit, not a real one.)*
3. **Staleness** — nothing found; all pages are dated 2026-08-12 through 2026-08-29 and
   internally consistent aside from the item above.
4. **Orphan / thin-page triage** — no genuine orphans; every page has at least one inbound
   link from an index or another page.
5. **Inline-artifact triage** — no standalone fenced code blocks found that belong in
   `scripts/` instead of prose.

## Todo Sync

- Added `office-air-quality-sensor` under `## Planned Projects` (was missing — `index-projects.md`
  already listed it as Planned, todo.md had never been synced to match)
- Added one `## Wiki Meta` item: `scripts/lint.py` needs porting

## Summary

**1 error-equivalent finding** (the planted contradiction — real once you ignore that it was
intentional), **0 warnings**, **1 process gap** (`scripts/lint.py` missing). No raw-file or
log archival candidates this run (`log.md` well under the 500-line threshold; no `raw_file:`
pages older than 14 days — everything here is from today or three weeks ago at most).
