{
  "name": "benchbook-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist && node scripts/check-privacy.mjs",
    "preview": "astro preview",
    "check:privacy": "node scripts/check-privacy.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.4",
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
